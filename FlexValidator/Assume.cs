namespace FlexValidator {
    /// <summary>
    /// Enum helper to assume a result when completing a validation
    /// </summary>
    public enum Assume {
        /// <summary>
        /// Assume the test to pass
        /// </summary>
        Pass,

        /// <summary>
        /// Assume the test to fail
        /// </summary>
        Fail,
    }
}